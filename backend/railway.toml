[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements/production.txt && python manage.py collectstatic --noinput && python manage.py migrate --noinput"

[deploy]
startCommand = "python manage.py check --deploy && gunicorn config.wsgi:application -c gunicorn.conf.py --log-level debug --error-logfile -"
